export interface Currency {
	r030: number;
	txt: string;
	rate: number;
	cc: string;
	exchangedate: string;
}
  
export interface SortedCurrency {
	title: string;
	rate: number;
	code: string;
}